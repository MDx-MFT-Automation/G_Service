<%= form_with(model: province, local: true) do |form| %>
  <% if province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(province.errors.count, "error") %> prohibited this province from being saved:</h2>

      <ul>
        <% province.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Province_Chinese_Name %>
    <%= form.text_field :Province_Chinese_Name %>
  </div>

  <div class="field">
    <%= form.label :Province_English_Name %>
    <%= form.text_field :Province_English_Name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
