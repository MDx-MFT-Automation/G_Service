<%= form_with(model: complain_status, local: true) do |form| %>
  <% if complain_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complain_status.errors.count, "error") %> prohibited this complain_status from being saved:</h2>

      <ul>
        <% complain_status.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Status %>
    <%= form.text_field :Status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
