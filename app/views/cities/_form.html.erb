<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
        <% city.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :City_Chinese_Name %>
    <%= form.text_field :City_Chinese_Name %>
  </div>

  <div class="field">
    <%= form.label :City_English_Name %>
    <%= form.text_field :City_English_Name %>
  </div>

  <div class="field">
    <%= form.label :province_id %>
    <%= form.collection_select(:province_id, Province.all, :id, :Province_Chinese_Name, {include_hidden: false, :prompt => "George tells you to select"}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
